options(
  styler.cache_root = ".",
  styler.colored_print.vertical = FALSE
)

# Use tidyverse style for styler addins by default
if (requireNamespace("styler", quietly = TRUE)) {
  options(styler.addins_style_transformer = styler::tidyverse_style)
}

